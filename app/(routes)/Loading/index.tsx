import { View, Text } from "react-native";
import React from "react";
import LoadingScreen from "@/screen/LoadingScreen/Loading.Screen";

const Loading = () => {
  return <LoadingScreen />;
};

export default Loading;
