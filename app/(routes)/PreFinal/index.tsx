import { View, Text } from "react-native";
import React from "react";
import PreFinalScreen from "@/screen/auth/PreFinalScreen/PreFinal.Screen";

const PreFinal = () => {
  return <PreFinalScreen />;
};

export default PreFinal;
