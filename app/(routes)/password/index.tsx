import { View, Text } from "react-native";
import React from "react";
import PasswordScreen from "@/screen/auth/PasswordScreen/Password.Screen";

const Password = () => {
  return <PasswordScreen />;
};

export default Password;
